# NetMatrix OpenAPI / REST API Reference

## GET /api/v1/topology
Returns the currently converged network router and switch graph topology.

## GET /api/v1/telemetry/metrics
Returns real-time Prometheus-formatted throughput, packet count, and jitter metrics.
