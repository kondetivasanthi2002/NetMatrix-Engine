# Production Deployment & Containerization Guide

NetMatrix can be deployed using standard Python environments, Docker containers, or Kubernetes Helm charts.

```bash
python -m netmatrix.api.rest_server
```
